#define _CRT_SECURE_NO_WARNINGS
#define <stdio.h>

int main(void) {
	int T, test_case;

	freopen("input (1).txt", "r", stdin);
	scanf("%d", &T);

	for (test_case = 1; test_case <= T; ++test_case) {
		int num[10];
		for (int)
	}
}